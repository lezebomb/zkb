APP_HOST=0.0.0.0
APP_PORT=8080
APP_VERSION=0.2.0
OFFLINE_MODE=true

MODEL_CLASSIFY_PATH=models/classifier.onnx
MODEL_DETECT_PATH=models/detector.onnx
MODEL_OCR_PATH=models/ocr

CLASSIFY_BACKEND=fallback
DETECT_BACKEND=fallback
OCR_BACKEND=fallback

DETECT_SCORE_THRESHOLD=0.25
DETECT_EMPTY_FALLBACK=false

IMAGE_DOWNLOAD_TIMEOUT_SECONDS=5
MAX_IMAGE_SIZE_MB=20

LOG_LEVEL=INFO
LOG_FILE=logs/app.log

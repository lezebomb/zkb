# Data Workspace

`raw/` and `processed/` are local workspaces and are excluded from submission packages. Use `data/dataset_manifest.yaml` and `docs/dataset_registry.md` to track sources before downloading anything large.

Default switches:

```env
ALLOW_DATASET_DOWNLOAD=false
RUN_FULL_TRAIN=false
```

